<clickhouse>
    <!--
        Single-node ClickHouse Keeper config for Langfuse.

        Langfuse v3 migrations create ReplicatedMergeTree tables, which require
        ZooKeeper or ClickHouse Keeper to be available. This config runs Keeper
        embedded inside the same ClickHouse server (single-node, single-replica)
        and points the <zookeeper> client at it. Production deployments would
        run a proper Keeper cluster.
    -->
    <keeper_server>
        <tcp_port>9181</tcp_port>
        <server_id>1</server_id>
        <log_storage_path>/var/lib/clickhouse/coordination/log</log_storage_path>
        <snapshot_storage_path>/var/lib/clickhouse/coordination/snapshots</snapshot_storage_path>
        <coordination_settings>
            <operation_timeout_ms>10000</operation_timeout_ms>
            <session_timeout_ms>30000</session_timeout_ms>
            <raft_logs_level>information</raft_logs_level>
        </coordination_settings>
        <raft_configuration>
            <server>
                <id>1</id>
                <hostname>localhost</hostname>
                <port>9234</port>
            </server>
        </raft_configuration>
    </keeper_server>

    <zookeeper>
        <node>
            <host>localhost</host>
            <port>9181</port>
        </node>
    </zookeeper>

    <macros>
        <shard>shard1</shard>
        <replica>replica1</replica>
    </macros>
</clickhouse>
